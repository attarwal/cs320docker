{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "OCaml",
            "type": "ocamldebug",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.d.byte",
            "stopOnEntry": false,
            "preLaunchTask": "build" // Build before launch        
        }
    ]
}